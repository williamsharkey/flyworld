{
  "name": "flyworld",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "prebuild": "node scripts/build-midi.mjs"
  },
  "dependencies": {
    "three": "^0.183.0",
    "lucide": "^0.577.0"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "@playwright/test": "^1.58.2"
  }
}
